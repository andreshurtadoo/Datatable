<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.10.24/css/dataTables.bootstrap5.min.css">
    <link rel="stylesheet" href="/style.css">
    <title>Data Tables</title>
  </head>
  <body>

    <main>

    <div class="container">

        <table id="example" class="table table-striped">
            <thead class="bg-dark text-light">
                <tr>
                    <th>Check</th>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Edad</th>
                    <th>Pais</th>
                    <th>Posicion</th>
                    <th>Codigo de Equipo</th>
                    <th class="text-center">
                       Accion
                    </th>
                </tr>
            </thead>
            
            <tbody>
                <% resultados.forEach((user) => { %>
                    <tr>
                        <td><input class="form-check-input" type="checkbox" id="check" value="<%= user.ID %>" name="desactivar[]" onchange="checkOne(this, value)" ></td>
                        <td><%= user.ID %> </td>
                        <td><%= user.Nombre %> </td>
                        <td><%= user.Edad %> </td>
                        <td><%= user.Pais %> </td>
                        <td><%= user.Posicion %> </td>
                        <td><%= user.COD_Equipo %> </td>
                        <td class="text-center">
                            <a href="/edit/<%= user.ID %>"      class="btn btn-outline-secondary" >Update</a>
                            <a href="/desactive/<%= user.ID %>" class="btn btn-outline-danger" id="delete" onclick="veryDelete()">Delete</a>
                        </td>
                    </tr>
                <% }) %>        
            </tbody>
        </table>

        <section class="container">

                <div class="row">
                    <div class="col">
                        <div>
                            <input class="form-check-input" type="checkbox" value="" id="checkAll" onchange="checkAll(this)">
                            <label class="form-check-label" for="flexCheckDefault">
                                Select All
                            </label>
                        </div>
                    </div>

                    <div class="col">
                        <div>
                            <a href="/create" class="btn btn-dark ">Create </a> 

                            <a href="#" class="btn btn-secondary" id="UpdateCheck" >Update</a>
                            <a href="#" class="btn btn-danger" id="deleteCheck" onclick="verifyCation()">Delete</a>
                            <a href="/eliminados" class="btn btn-primary" id="eliminados" >Eliminated</a>
                        </div>
                    </div>

                    <div class="col">
                        <div>
                            <span class="fs-4">MAX 100 - </span>
                            <span id="span" class="fs-4">0</span>
                            <span id="alert" class="fs-4 text-danger"></span>
                        </div>
                    </div>

                </div>
        
        </section>
    
    </div>

    </main>


    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script src="https://cdn.datatables.net/1.10.24/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.10.24/js/dataTables.bootstrap5.min.js"></script>
    <script src="./script.js"></script>
   
        
</body>
</html>


